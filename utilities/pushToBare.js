/**
 * A utility that just implement `git push path(projects/projName.git) master` - bare repo path.
 * 
 * 1. implement node-git-server for listening to push cli event
 * 2. repos.on('push', < perform a push to bare repo >)
 */


